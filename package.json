{
  "name": "zustand-sync-monorepo",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter @zustand-sync/demo dev",
    "dev:server": "tsx watch apps/demo/server.ts",
    "build": "pnpm --filter \"./packages/*\" build"
  },
  "pnpm": {
    "overrides": {
      "zustand": "5.0.7"
    }
  },
  "devDependencies": {
    "typescript": "^5.2.2",
    "tsx": "^4.20.3"
  }
}

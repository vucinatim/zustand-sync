{
  "name": "zustand-sync-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build:packages": "tsc -b packages/core packages/client packages/server",
    "build": "echo '--- BUILDING CORE ---' && cd packages/core && pnpm build && cd ../.. && echo '--- CORE BUILD COMPLETE. LISTING FILES ---' && ls -R && echo '--- BUILDING CLIENT ---' && cd packages/client && pnpm build && cd ../.. && echo '--- CLIENT BUILD COMPLETE. LISTING FILES ---' && ls -R && echo '--- BUILDING SERVER ---' && cd packages/server && pnpm build && cd ../.. && echo '--- SERVER BUILD COMPLETE. LISTING FILES ---' && ls -R && echo '--- BUILDING DEMO APP ---' && pnpm --filter @zustand-sync/demo build",
    "start": "pnpm --filter @zustand-sync/demo start",
    "dev": "pnpm --filter @zustand-sync/demo dev"
  },
  "pnpm": {
    "overrides": {
      "zustand": "5.0.7"
    }
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "typescript": "~5.8.3"
  }
}

{
  "name": "zustand-sync-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "tsc -b apps/demo && pnpm --filter @zustand-sync/demo run build:client",
    "start": "cd apps/demo && pnpm start",
    "dev": "pnpm --filter @zustand-sync/demo dev"
  },
  "pnpm": {
    "overrides": {
      "zustand": "5.0.7"
    }
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "typescript": "~5.8.3"
  }
}

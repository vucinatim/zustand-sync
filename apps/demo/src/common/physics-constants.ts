// Physics constants for easy adjustment
export const PHYSICS_CONSTANTS = {
  GRAVITY: 1900, // How fast the character falls (higher = faster fall)
  MOVE_SPEED: 600, // How fast the character moves left/right
  JUMP_VELOCITY: -600, // How high the character jumps (more negative = higher jump)
} as const;
